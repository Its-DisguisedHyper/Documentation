This is a very minor update to correct some issues that are present in the current `1.8.1` update. This update resolves any further reported errors.

This update only requires you to replace the; `index.js`, `backend.js`, `updatemanager.js`, and the `/views` folder.
---
**Changes:**
- Updated the way general page creation works
- Fixed a bug that would cause a site crash
- Fixed a bug that caused editing store listings to break
- Fixed an SQL Audit Log error that caused SQL errors in the console
- Fixed multiple other bugs